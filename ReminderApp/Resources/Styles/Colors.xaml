<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- ========================= -->
    <!-- CORE BRAND COLORS (MOBILE) -->
    <!-- ========================= -->

    <!-- Primary: Deep Focus Blue (anchor / trust) -->
    <Color x:Key="Primary">#1E2A38</Color>
    <Color x:Key="PrimaryDark">#16202B</Color>
    <Color x:Key="PrimaryDarkText">#E8EDF2</Color>

    <!-- Secondary: Intent Purple (CTA / focus) -->
    <Color x:Key="Secondary">#6B5CFF</Color>
    <Color x:Key="SecondaryDarkText">#FFFFFF</Color>

    <!-- Tertiary: Signal Mint (success / progress) -->
    <Color x:Key="Tertiary">#2ECC9A</Color>

    <!-- ========================= -->
    <!-- NEUTRAL -->
    <!-- ========================= -->

    <Color x:Key="White">#FFFFFF</Color>
    <Color x:Key="Black">#000000</Color>

    <!-- Kill Magenta biar gak norak -->
    <Color x:Key="Magenta">#6B5CFF</Color>

    <!-- Legacy naming, tapi disamain -->
    <Color x:Key="MidnightBlue">#1E2A38</Color>
    <Color x:Key="OffBlack">#121821</Color>

    <!-- ========================= -->
    <!-- GRAYS (UI SAFE) -->
    <!-- ========================= -->

    <Color x:Key="Gray100">#F1F3F5</Color>
    <Color x:Key="Gray200">#E2E6EA</Color>
    <Color x:Key="Gray300">#CED4DA</Color>
    <Color x:Key="Gray400">#ADB5BD</Color>
    <Color x:Key="Gray500">#868E96</Color>
    <Color x:Key="Gray600">#495057</Color>
    <Color x:Key="Gray900">#212529</Color>
    <Color x:Key="Gray950">#161A1D</Color>

    <!-- ========================= -->
    <!-- BRUSHES (JANGAN DIUBAH STRUKTUR) -->
    <!-- ========================= -->

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource Secondary}"/>
    <SolidColorBrush x:Key="TertiaryBrush" Color="{StaticResource Tertiary}"/>
    <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource White}"/>
    <SolidColorBrush x:Key="BlackBrush" Color="{StaticResource Black}"/>

    <SolidColorBrush x:Key="Gray100Brush" Color="{StaticResource Gray100}"/>
    <SolidColorBrush x:Key="Gray200Brush" Color="{StaticResource Gray200}"/>
    <SolidColorBrush x:Key="Gray300Brush" Color="{StaticResource Gray300}"/>
    <SolidColorBrush x:Key="Gray400Brush" Color="{StaticResource Gray400}"/>
    <SolidColorBrush x:Key="Gray500Brush" Color="{StaticResource Gray500}"/>
    <SolidColorBrush x:Key="Gray600Brush" Color="{StaticResource Gray600}"/>
    <SolidColorBrush x:Key="Gray900Brush" Color="{StaticResource Gray900}"/>
    <SolidColorBrush x:Key="Gray950Brush" Color="{StaticResource Gray950}"/>

</ResourceDictionary>
